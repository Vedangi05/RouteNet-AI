apiVersion: v1
kind: Service
metadata:
  name: inference-service
  labels:
    app: inference-worker
spec:
  type: ClusterIP  # Internal service for router to access
  selector:
    app: inference-worker
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
